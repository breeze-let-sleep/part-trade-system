$kind: http-example
request:
  url: "{{baseUrl}}/collects/likeCollects?inputMerchant=小明&customerId=200001&currentPage=1&pageSize=10"
  method: GET
  headers:
    - key: Accept
      value: "*/*"
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: inputMerchant
      value: 小明
      description: 模糊匹配的供应商名称
    - key: customerId
      value: "200001"
      description: 顾客id
    - key: currentPage
      value: "1"
      description: 当前页，默认为1
    - key: pageSize
      value: "10"
      description: 每页大小，默认为10
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: "*/*"
  body:
    type: text
    content: |-
      {
        "code": 1,
        "msg": "success",
        "data": {}
      }
order: 1000
